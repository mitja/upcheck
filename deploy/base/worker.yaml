# Celery worker — runs the HTTP checks. Same image and env as web.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  labels:
    app: upcheck
    component: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: upcheck
      component: worker
  template:
    metadata:
      labels:
        app: upcheck
        component: worker
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
      containers:
        - name: worker
          image: ghcr.io/mitja/upcheck:latest
          imagePullPolicy: IfNotPresent
          # Threads pool: checks are I/O-bound (HTTP waits). A concurrency-2
          # prefork worker saturates once a few % of targets are slow/timing
          # out; 16 threads stay in one process and fit the memory limit.
          command: ["celery", "-A", "project", "worker", "-l", "INFO", "--pool", "threads", "--concurrency", "16"]
          envFrom:
            - configMapRef: { name: upcheck-config }
            - secretRef: { name: upcheck-secrets }
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef: { name: pg-app, key: uri }
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 512Mi
